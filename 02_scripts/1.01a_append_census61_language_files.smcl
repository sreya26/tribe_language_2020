{smcl}
{txt}{sf}{ul off}{.-}
      name:  {res}plog_766
       {txt}log:  {res}/Users/sreyamajumder/Dropbox/sreya_majumder/tribelanguage_2020 sreya/02_scripts/1.01a_append_census61_language_files.smcl
  {txt}log type:  {res}smcl
 {txt}opened on:  {res} 6 Sep 2021, 20:07:01
{txt}
{com}. ********************************************************************************************************************************
. * this do-file constructs the mother tongue data from Census 1961 data 
. ********************************************************************************************************************************
. project , original("../01_data/census_1961_st_language_tables/an61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/an61langst01.xlsx" filesig(1869954139:10463){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/ap61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/ap61langst01.xlsx" filesig(699701404:156472){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/as61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/as61langst01.xlsx" filesig(3101273424:181095){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/br61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/br61langst01.xlsx" filesig(85274153:548974){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/dn61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/dn61langst01.xlsx" filesig(453856978:11250){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/gj61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/gj61langst01.xlsx" filesig(1317028844:141253){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/hp61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/hp61langst01.xlsx" filesig(1564807698:40530){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/ke61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/ke61langst01.xlsx" filesig(3231426306:39010){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/kt61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/kt61langst01.xlsx" filesig(3406074553:127487){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/lm61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/lm61langst01.xlsx" filesig(1260351446:12309){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/mh61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/mh61langst01.xlsx" filesig(172120606:804247){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/mn61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/mn61langst01.xlsx" filesig(1183088826:98193){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/mp61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/mp61langst01.xlsx" filesig(2644125529:1467782){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/ng61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/ng61langst01.xlsx" filesig(1148482948:36963){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/or61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/or61langst01.xlsx" filesig(1806429792:224515){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/pn61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/pn61langst01.xlsx" filesig(2304870226:6666){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/rj61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/rj61langst01.xlsx" filesig(368284558:83656){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/tn61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/tn61langst01.xlsx" filesig(2271818974:52526){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/tr61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/tr61langst01.xlsx" filesig(911877445:144060){txt}

{com}. project , original("../01_data/census_1961_st_language_tables/wb61langst01.xlsx")
{txt}project version2_tribe_language > do-file uses original: {res}"01_data/census_1961_st_language_tables/wb61langst01.xlsx" filesig(2122287908:802664){txt}

{com}. 
.  
. save "../08_temp/census1961_st_mothertongue", emptyok replace
{txt}(dataset contains 0 observations)
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}

{com}. 
. local states an ap as br dn gj hp ke kt lm mh mn mp ng or pn rj tn tr wb
{txt}
{com}. 
. foreach st of local states {c -(}
{txt}  2{com}.         import excel using "../01_data/census_1961_st_language_tables/`st'61langst01.xlsx", firstrow case(lower) clear
{txt}  3{com}.         foreach var of varlist subsidiary_speakers_* sl_* {c -(}
{txt}  4{com}.                 capture replace `var' = "0" if `var' == "nil"
{txt}  5{com}.                 {c )-}
{txt}  6{com}.         destring subsidiary_speakers_* sl_*, ignore("xXN") replace
{txt}  7{com}.         capture drop ?
{txt}  8{com}.         capture drop ??
{txt}  9{com}.         dis as err "Appending the parent file to the file for state `st'..."
{txt} 10{com}.         append using "../08_temp/census1961_st_mothertongue"
{txt} 11{com}.         save "../08_temp/census1961_st_mothertongue", replace
{txt} 12{com}. {c )-}
{res}{text}(21 vars, 18 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_eng_m already numeric; no {res}replace
{txt}sl_eng_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_hindustani_m already numeric; no {res}replace
{txt}sl_hindustani_f already numeric; no {res}replace
{txt}sl_japanese_m already numeric; no {res}replace
{txt}sl_japanese_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state an...
{txt}{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(64 vars, 660 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_banjari_m already numeric; no {res}replace
{txt}sl_banjari_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_chachadi_m already numeric; no {res}replace
{txt}sl_chachadi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gondi_m already numeric; no {res}replace
{txt}sl_gondi_f already numeric; no {res}replace
{txt}sl_gadaba_m already numeric; no {res}replace
{txt}sl_gadaba_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_jatapu_m already numeric; no {res}replace
{txt}sl_jatapu_f already numeric; no {res}replace
{txt}sl_lamanikanbadu_m already numeric; no {res}replace
{txt}sl_lamanikanbadu_f already numeric; no {res}replace
{txt}sl_lamanilambadi_m already numeric; no {res}replace
{txt}sl_lamanilambadi_f already numeric; no {res}replace
{txt}sl_khondkondh_m already numeric; no {res}replace
{txt}sl_khondkondh_f already numeric; no {res}replace
{txt}sl_kannada_m already numeric; no {res}replace
{txt}sl_kannada_f already numeric; no {res}replace
{txt}sl_koya_m already numeric; no {res}replace
{txt}sl_koya_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_malayalam_m already numeric; no {res}replace
{txt}sl_malayalam_f already numeric; no {res}replace
{txt}sl_manne_m already numeric; no {res}replace
{txt}sl_manne_f already numeric; no {res}replace
{txt}sl_marwari_m already numeric; no {res}replace
{txt}sl_marwari_f already numeric; no {res}replace
{txt}sl_oriya_m already numeric; no {res}replace
{txt}sl_oriya_f already numeric; no {res}replace
{txt}sl_punjabi_m already numeric; no {res}replace
{txt}sl_punjabi_f already numeric; no {res}replace
{txt}sl_sindhi_m already numeric; no {res}replace
{txt}sl_sindhi_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_savara_m already numeric; no {res}replace
{txt}sl_savara_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{txt}sl_yerukalayerukula_m already numeric; no {res}replace
{txt}sl_yerukalayerukula_f already numeric; no {res}replace
{txt}sl_yenadiyanadi_m already numeric; no {res}replace
{txt}sl_yenadiyanadi_f already numeric; no {res}replace
{err}Appending the parent file to the file for state ap...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str14}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str14}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(96 vars, 521 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_burmese_m already numeric; no {res}replace
{txt}sl_burmese_f already numeric; no {res}replace
{txt}sl_bhoikhasi_m already numeric; no {res}replace
{txt}sl_bhoikhasi_f already numeric; no {res}replace
{txt}sl_bodoboro_m already numeric; no {res}replace
{txt}sl_bodoboro_f already numeric; no {res}replace
{txt}sl_caro_m already numeric; no {res}replace
{txt}sl_caro_f already numeric; no {res}replace
{txt}sl_chakma_m already numeric; no {res}replace
{txt}sl_chakma_f already numeric; no {res}replace
{txt}sl_chinesechini_m already numeric; no {res}replace
{txt}sl_chinesechini_f already numeric; no {res}replace
{txt}sl_chinunspecified_m already numeric; no {res}replace
{txt}sl_chinunspecified_f already numeric; no {res}replace
{txt}sl_deori_m already numeric; no {res}replace
{txt}sl_deori_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_french_m already numeric; no {res}replace
{txt}sl_french_f already numeric; no {res}replace
{txt}sl_garo_m already numeric; no {res}replace
{txt}sl_garo_f already numeric; no {res}replace
{txt}sl_govari_m already numeric; no {res}replace
{txt}sl_govari_f already numeric; no {res}replace
{txt}sl_haijonghajong_m already numeric; no {res}replace
{txt}sl_haijonghajong_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_hmar_m already numeric; no {res}replace
{txt}sl_hmar_f already numeric; no {res}replace
{txt}sl_kachari_m already numeric; no {res}replace
{txt}sl_kachari_f already numeric; no {res}replace
{txt}sl_khasi_m already numeric; no {res}replace
{txt}sl_khasi_f already numeric; no {res}replace
{txt}sl_khawathlang_m already numeric; no {res}replace
{txt}sl_khawathlang_f already numeric; no {res}replace
{txt}sl_khelma_m already numeric; no {res}replace
{txt}sl_khelma_f already numeric; no {res}replace
{txt}sl_kukiunspecified_m already numeric; no {res}replace
{txt}sl_kukiunspecified_f already numeric; no {res}replace
{txt}sl_lai_m already numeric; no {res}replace
{txt}sl_lai_f already numeric; no {res}replace
{txt}sl_lushaimizo_m already numeric; no {res}replace
{txt}sl_lushaimizo_f already numeric; no {res}replace
{txt}sl_lakher_m already numeric; no {res}replace
{txt}sl_lakher_f already numeric; no {res}replace
{txt}sl_lalung_m already numeric; no {res}replace
{txt}sl_lalung_f already numeric; no {res}replace
{txt}sl_miri_m already numeric; no {res}replace
{txt}sl_miri_f already numeric; no {res}replace
{txt}sl_mech_m already numeric; no {res}replace
{txt}sl_mech_f already numeric; no {res}replace
{txt}sl_manipurimeithei_m already numeric; no {res}replace
{txt}sl_manipurimeithei_f already numeric; no {res}replace
{txt}sl_mikir_m already numeric; no {res}replace
{txt}sl_mikir_f already numeric; no {res}replace
{txt}sl_mogh_m already numeric; no {res}replace
{txt}sl_mogh_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_nagaunspecified_m already numeric; no {res}replace
{txt}sl_nagaunspecified_f already numeric; no {res}replace
{txt}sl_pnarsynteng_m already numeric; no {res}replace
{txt}sl_pnarsynteng_f already numeric; no {res}replace
{txt}sl_pawi_m already numeric; no {res}replace
{txt}sl_pawi_f already numeric; no {res}replace
{txt}sl_paite_m already numeric; no {res}replace
{txt}sl_paite_f already numeric; no {res}replace
{txt}sl_ralte_m already numeric; no {res}replace
{txt}sl_ralte_f already numeric; no {res}replace
{txt}sl_rabha_m already numeric; no {res}replace
{txt}sl_rabha_f already numeric; no {res}replace
{txt}sl_reang_m already numeric; no {res}replace
{txt}sl_reang_f already numeric; no {res}replace
{txt}sl_santali_m already numeric; no {res}replace
{txt}sl_santali_f already numeric; no {res}replace
{txt}sl_takam_m already numeric; no {res}replace
{txt}sl_takam_f already numeric; no {res}replace
{txt}sl_tripuri_m already numeric; no {res}replace
{txt}sl_tripuri_f already numeric; no {res}replace
{err}Appending the parent file to the file for state as...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str5}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str16}, now {bf:str17} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(88 vars, 1,794 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_arabicarbi_m already numeric; no {res}replace
{txt}sl_arabicarbi_f already numeric; no {res}replace
{txt}sl_asuri_m already numeric; no {res}replace
{txt}sl_asuri_f already numeric; no {res}replace
{txt}sl_awadhi_m already numeric; no {res}replace
{txt}sl_awadhi_f already numeric; no {res}replace
{txt}sl_beriya_m already numeric; no {res}replace
{txt}sl_beriya_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_bhojpuri_m already numeric; no {res}replace
{txt}sl_bhojpuri_f already numeric; no {res}replace
{txt}sl_bhumij_m already numeric; no {res}replace
{txt}sl_bhumij_f already numeric; no {res}replace
{txt}sl_bilaspuri_m already numeric; no {res}replace
{txt}sl_bilaspuri_f already numeric; no {res}replace
{txt}sl_birjiabrijiabinjhia_m already numeric; no {res}replace
{txt}sl_birjiabrijiabinjhia_f already numeric; no {res}replace
{txt}sl_dhangri_m already numeric; no {res}replace
{txt}sl_dhangri_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gawari_m already numeric; no {res}replace
{txt}sl_gawari_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_ho_m already numeric; no {res}replace
{txt}sl_ho_f already numeric; no {res}replace
{txt}sl_italian_m already numeric; no {res}replace
{txt}sl_italian_f already numeric; no {res}replace
{txt}sl_karmali_m already numeric; no {res}replace
{txt}sl_karmali_f already numeric; no {res}replace
{txt}sl_kharia_m already numeric; no {res}replace
{txt}sl_kharia_f already numeric; no {res}replace
{txt}sl_kherwari_m already numeric; no {res}replace
{txt}sl_kherwari_f already numeric; no {res}replace
{txt}sl_khorthakhotta_m already numeric; no {res}replace
{txt}sl_khorthakhotta_f already numeric; no {res}replace
{txt}sl_kol_m already numeric; no {res}replace
{txt}sl_kol_f already numeric; no {res}replace
{txt}sl_korwa_m already numeric; no {res}replace
{txt}sl_korwa_f already numeric; no {res}replace
{txt}sl_kurukhoraon_m already numeric; no {res}replace
{txt}sl_kurukhoraon_f already numeric; no {res}replace
{txt}sl_lohra_m already numeric; no {res}replace
{txt}sl_lohra_f already numeric; no {res}replace
{txt}sl_magahimagadhi_m already numeric; no {res}replace
{txt}sl_magahimagadhi_f already numeric; no {res}replace
{txt}sl_mahli_m already numeric; no {res}replace
{txt}sl_mahli_f already numeric; no {res}replace
{txt}sl_malto_m already numeric; no {res}replace
{txt}sl_malto_f already numeric; no {res}replace
{txt}sl_mundari_m already numeric; no {res}replace
{txt}sl_mundari_f already numeric; no {res}replace
{txt}sl_nagpuri_eastern_magahi_m already numeric; no {res}replace
{txt}sl_nagpuri_eastern_magahi_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_oriya_m already numeric; no {res}replace
{txt}sl_oriya_f already numeric; no {res}replace
{txt}sl_panchpargania_m already numeric; no {res}replace
{txt}sl_panchpargania_f already numeric; no {res}replace
{txt}sl_persian_m already numeric; no {res}replace
{txt}sl_persian_f already numeric; no {res}replace
{txt}sl_sadansadri_m already numeric; no {res}replace
{txt}sl_sadansadri_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_santali_m already numeric; no {res}replace
{txt}sl_santali_f already numeric; no {res}replace
{txt}sl_tamaria_m already numeric; no {res}replace
{txt}sl_tamaria_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state br...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str5}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str21}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str15}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_nepali_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(22 vars, 29 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_portuguese_m already numeric; no {res}replace
{txt}sl_portuguese_f already numeric; no {res}replace
{txt}sl_varli_m already numeric; no {res}replace
{txt}sl_varli_f already numeric; no {res}replace
{err}Appending the parent file to the file for state dn...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str22}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str22}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str26}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str17}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_marathi_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(34 vars, 1,050 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_dangi_m already numeric; no {res}replace
{txt}sl_dangi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_koknakoknikukna_m already numeric; no {res}replace
{txt}sl_koknakoknikukna_f already numeric; no {res}replace
{txt}sl_kachchhi_m already numeric; no {res}replace
{txt}sl_kachchhi_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_od_m already numeric; no {res}replace
{txt}sl_od_f already numeric; no {res}replace
{txt}sl_rajasthani_m already numeric; no {res}replace
{txt}sl_rajasthani_f already numeric; no {res}replace
{txt}sl_varli_m already numeric; no {res}replace
{txt}sl_varli_f already numeric; no {res}replace
{txt}sl_vasava_m already numeric; no {res}replace
{txt}sl_vasava_f already numeric; no {res}replace
{err}Appending the parent file to the file for state gj...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str7}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str15}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str152}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str17}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(24 vars, 345 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_arabicarbi_m already numeric; no {res}replace
{txt}sl_arabicarbi_f already numeric; no {res}replace
{txt}sl_chinesechini_m already numeric; no {res}replace
{txt}sl_chinesechini_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_punjabi_m already numeric; no {res}replace
{txt}sl_punjabi_f already numeric; no {res}replace
{txt}sl_tibetan_m already numeric; no {res}replace
{txt}sl_tibetan_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state hp...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str16}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str16}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str35}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str18}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(28 vars, 294 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_ceylonesesimelusinghalese_m already numeric; no {res}replace
{txt}sl_ceylonesesimelusinghalese_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_kannada_m already numeric; no {res}replace
{txt}sl_kannada_f already numeric; no {res}replace
{txt}sl_malayalam_m already numeric; no {res}replace
{txt}sl_malayalam_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_tulu_m already numeric; no {res}replace
{txt}sl_tulu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state ke...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str6}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str10}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str28}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str13}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(42 vars, 763 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_aravu_m already numeric; no {res}replace
{txt}sl_aravu_f already numeric; no {res}replace
{txt}sl_banjarilambani_m already numeric; no {res}replace
{txt}sl_banjarilambani_f already numeric; no {res}replace
{txt}sl_coorgi_m already numeric; no {res}replace
{txt}sl_coorgi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_kannada_m already numeric; no {res}replace
{txt}sl_kannada_f already numeric; no {res}replace
{txt}sl_konkani_m already numeric; no {res}replace
{txt}sl_konkani_f already numeric; no {res}replace
{txt}sl_malayalam_m already numeric; no {res}replace
{txt}sl_malayalam_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_parsi_m already numeric; no {res}replace
{txt}sl_parsi_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_tulu_m already numeric; no {res}replace
{txt}sl_tulu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state kt...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str6}, now {bf:str25} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str15}, now {bf:str34} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str152}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str18}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(29 vars, 22 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_arabicarbi_m already numeric; no {res}replace
{txt}sl_arabicarbi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_mahlmaldiviantulu_m already numeric; no {res}replace
{txt}sl_mahlmaldiviantulu_f already numeric; no {res}replace
{txt}sl_malayalam_m already numeric; no {res}replace
{txt}sl_malayalam_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_tulu_m already numeric; no {res}replace
{txt}sl_tulu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state lm...
{txt}{p 0 7 2}
(variable
{bf:tribe} was {bf:str115}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str14}, now {bf:str22} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_malayalam_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_malayalam_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tulu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tulu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(92 vars, 2,529 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_ahirani_m already numeric; no {res}replace
{txt}sl_ahirani_f already numeric; no {res}replace
{txt}sl_arabicarbi_m already numeric; no {res}replace
{txt}sl_arabicarbi_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_banjari_m already numeric; no {res}replace
{txt}sl_banjari_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_bhili_m already numeric; no {res}replace
{txt}sl_bhili_f already numeric; no {res}replace
{txt}sl_chhattisgarhi_m already numeric; no {res}replace
{txt}sl_chhattisgarhi_f already numeric; no {res}replace
{txt}sl_dhamdi_m already numeric; no {res}replace
{txt}sl_dhamdi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gondi_m already numeric; no {res}replace
{txt}sl_gondi_f already numeric; no {res}replace
{txt}sl_govari_m already numeric; no {res}replace
{txt}sl_govari_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_gujaru_m already numeric; no {res}replace
{txt}sl_gujaru_f already numeric; no {res}replace
{txt}sl_halabi_m already numeric; no {res}replace
{txt}sl_halabi_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_iraniiranian_m already numeric; no {res}replace
{txt}sl_iraniiranian_f already numeric; no {res}replace
{txt}sl_kannada_m already numeric; no {res}replace
{txt}sl_kannada_f already numeric; no {res}replace
{txt}sl_kathodi_m already numeric; no {res}replace
{txt}sl_kathodi_f already numeric; no {res}replace
{txt}sl_katkari_m already numeric; no {res}replace
{txt}sl_katkari_f already numeric; no {res}replace
{txt}sl_kolami_m already numeric; no {res}replace
{txt}sl_kolami_f already numeric; no {res}replace
{txt}sl_konkani_m already numeric; no {res}replace
{txt}sl_konkani_f already numeric; no {res}replace
{txt}sl_korku_m already numeric; no {res}replace
{txt}sl_korku_f already numeric; no {res}replace
{txt}sl_koshti_marathi_m already numeric; no {res}replace
{txt}sl_koshti_marathi_f already numeric; no {res}replace
{txt}sl_koya_m already numeric; no {res}replace
{txt}sl_koya_f already numeric; no {res}replace
{txt}sl_lamanilambadi_m already numeric; no {res}replace
{txt}sl_lamanilambadi_f already numeric; no {res}replace
{txt}sl_madgi_m already numeric; no {res}replace
{txt}sl_madgi_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_maria_m already numeric; no {res}replace
{txt}sl_maria_f already numeric; no {res}replace
{txt}sl_marwari_m already numeric; no {res}replace
{txt}sl_marwari_f already numeric; no {res}replace
{txt}sl_mawchi_m already numeric; no {res}replace
{txt}sl_mawchi_f already numeric; no {res}replace
{txt}sl_nimadi_m already numeric; no {res}replace
{txt}sl_nimadi_f already numeric; no {res}replace
{txt}sl_padvi_m already numeric; no {res}replace
{txt}sl_padvi_f already numeric; no {res}replace
{txt}sl_paradhi_m already numeric; no {res}replace
{txt}sl_paradhi_f already numeric; no {res}replace
{txt}sl_pardesi_m already numeric; no {res}replace
{txt}sl_pardesi_f already numeric; no {res}replace
{txt}sl_pawri_m already numeric; no {res}replace
{txt}sl_pawri_f already numeric; no {res}replace
{txt}sl_punjabi_m already numeric; no {res}replace
{txt}sl_punjabi_f already numeric; no {res}replace
{txt}sl_sindhi_m already numeric; no {res}replace
{txt}sl_sindhi_f already numeric; no {res}replace
{txt}sl_tadavi_m already numeric; no {res}replace
{txt}sl_tadavi_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state mh...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str11}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str19}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str523}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_arabicarbi_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_konkani_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_lamanilambadi_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_lamanilambadi_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_koya_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_koya_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(79 vars, 324 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_anal_m already numeric; no {res}replace
{txt}sl_anal_f already numeric; no {res}replace
{txt}sl_angami_m already numeric; no {res}replace
{txt}sl_angami_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_burmese_m already numeric; no {res}replace
{txt}sl_burmese_f already numeric; no {res}replace
{txt}sl_chakasang_m already numeric; no {res}replace
{txt}sl_chakasang_f already numeric; no {res}replace
{txt}sl_chiru_m already numeric; no {res}replace
{txt}sl_chiru_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gangte_m already numeric; no {res}replace
{txt}sl_gangte_f already numeric; no {res}replace
{txt}sl_gurkha_m already numeric; no {res}replace
{txt}sl_gurkha_f already numeric; no {res}replace
{txt}sl_hmar_m already numeric; no {res}replace
{txt}sl_hmar_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_kabui_m already numeric; no {res}replace
{txt}sl_kabui_f already numeric; no {res}replace
{txt}sl_kachanaga_m already numeric; no {res}replace
{txt}sl_kachanaga_f already numeric; no {res}replace
{txt}sl_kom_m already numeric; no {res}replace
{txt}sl_kom_f already numeric; no {res}replace
{txt}sl_kukiunspecified_m already numeric; no {res}replace
{txt}sl_kukiunspecified_f already numeric; no {res}replace
{txt}sl_leimei_m already numeric; no {res}replace
{txt}sl_leimei_f already numeric; no {res}replace
{txt}sl_lushaimizo_m already numeric; no {res}replace
{txt}sl_lushaimizo_f already numeric; no {res}replace
{txt}sl_manipurimeithei_m already numeric; no {res}replace
{txt}sl_manipurimeithei_f already numeric; no {res}replace
{txt}sl_mao_m already numeric; no {res}replace
{txt}sl_mao_f already numeric; no {res}replace
{txt}sl_monsang_m already numeric; no {res}replace
{txt}sl_monsang_f already numeric; no {res}replace
{txt}sl_moyon_m already numeric; no {res}replace
{txt}sl_moyon_f already numeric; no {res}replace
{txt}sl_mizo_m already numeric; no {res}replace
{txt}sl_mizo_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_paite_m already numeric; no {res}replace
{txt}sl_paite_f already numeric; no {res}replace
{txt}sl_rongmei_m already numeric; no {res}replace
{txt}sl_rongmei_f already numeric; no {res}replace
{txt}sl_simte_m already numeric; no {res}replace
{txt}sl_simte_f already numeric; no {res}replace
{txt}sl_thado_m already numeric; no {res}replace
{txt}sl_thado_f already numeric; no {res}replace
{txt}sl_tangkhul_m already numeric; no {res}replace
{txt}sl_tangkhul_f already numeric; no {res}replace
{txt}sl_tidim_m already numeric; no {res}replace
{txt}sl_tidim_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{txt}sl_vaiphui_m already numeric; no {res}replace
{txt}sl_vaiphui_f already numeric; no {res}replace
{txt}sl_zou_m already numeric; no {res}replace
{txt}sl_zou_f already numeric; no {res}replace
{err}Appending the parent file to the file for state mn...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str7}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str17}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str24}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str16}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_nepali_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_burmese_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(152 vars, 3,066 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_agaria_m already numeric; no {res}replace
{txt}sl_agaria_f already numeric; no {res}replace
{txt}sl_arabicarbi_m already numeric; no {res}replace
{txt}sl_arabicarbi_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_asuri_m already numeric; no {res}replace
{txt}sl_asuri_f already numeric; no {res}replace
{txt}sl_baghelkhandi_m already numeric; no {res}replace
{txt}sl_baghelkhandi_f already numeric; no {res}replace
{txt}sl_baigani_m already numeric; no {res}replace
{txt}sl_baigani_f already numeric; no {res}replace
{txt}sl_banjari_m already numeric; no {res}replace
{txt}sl_banjari_f already numeric; no {res}replace
{txt}sl_barel_m already numeric; no {res}replace
{txt}sl_barel_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_bharia_m already numeric; no {res}replace
{txt}sl_bharia_f already numeric; no {res}replace
{txt}sl_bhatri_m already numeric; no {res}replace
{txt}sl_bhatri_f already numeric; no {res}replace
{txt}sl_bhilali_m already numeric; no {res}replace
{txt}sl_bhilali_f already numeric; no {res}replace
{txt}sl_bhili_m already numeric; no {res}replace
{txt}sl_bhili_f already numeric; no {res}replace
{txt}sl_bhojpuri_m already numeric; no {res}replace
{txt}sl_bhojpuri_f already numeric; no {res}replace
{txt}sl_bihari_m already numeric; no {res}replace
{txt}sl_bihari_f already numeric; no {res}replace
{txt}sl_binjhwari_m already numeric; no {res}replace
{txt}sl_binjhwari_f already numeric; no {res}replace
{txt}sl_bundelkhandi_m already numeric; no {res}replace
{txt}sl_bundelkhandi_f already numeric; no {res}replace
{txt}sl_chhattisgarhi_m already numeric; no {res}replace
{txt}sl_chhattisgarhi_f already numeric; no {res}replace
{txt}sl_dhangri_m already numeric; no {res}replace
{txt}sl_dhangri_f already numeric; no {res}replace
{txt}sl_dhurwa_m already numeric; no {res}replace
{txt}sl_dhurwa_f already numeric; no {res}replace
{txt}sl_dorli_m already numeric; no {res}replace
{txt}sl_dorli_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_ghati_m already numeric; no {res}replace
{txt}sl_ghati_f already numeric; no {res}replace
{txt}sl_gondani_m already numeric; no {res}replace
{txt}sl_gondani_f already numeric; no {res}replace
{txt}sl_gondi_m already numeric; no {res}replace
{txt}sl_gondi_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_gurmukhi_m already numeric; no {res}replace
{txt}sl_gurmukhi_f already numeric; no {res}replace
{txt}sl_halabi_m already numeric; no {res}replace
{txt}sl_halabi_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_kamari_m already numeric; no {res}replace
{txt}sl_kamari_f already numeric; no {res}replace
{txt}sl_katia_m: all characters numeric; {res}replaced {txt}as {res}byte
{txt}(3054 missing values generated)
{res}{txt}sl_katia_f already numeric; no {res}replace
{txt}sl_khandeshi_m already numeric; no {res}replace
{txt}sl_khandeshi_f already numeric; no {res}replace
{txt}sl_kharia_m already numeric; no {res}replace
{txt}sl_kharia_f already numeric; no {res}replace
{txt}sl_kol_m already numeric; no {res}replace
{txt}sl_kol_f already numeric; no {res}replace
{txt}sl_konda_m already numeric; no {res}replace
{txt}sl_konda_f already numeric; no {res}replace
{txt}sl_korku_m already numeric; no {res}replace
{txt}sl_korku_f already numeric; no {res}replace
{txt}sl_korwa_m already numeric; no {res}replace
{txt}sl_korwa_f already numeric; no {res}replace
{txt}sl_kurukhoraon_m already numeric; no {res}replace
{txt}sl_kurukhoraon_f already numeric; no {res}replace
{txt}sl_mahari_m already numeric; no {res}replace
{txt}sl_mahari_f already numeric; no {res}replace
{txt}sl_majhikorwa_m already numeric; no {res}replace
{txt}sl_majhikorwa_f already numeric; no {res}replace
{txt}sl_malvi_m already numeric; no {res}replace
{txt}sl_malvi_f already numeric; no {res}replace
{txt}sl_mankari_m already numeric; no {res}replace
{txt}sl_mankari_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_maria_m already numeric; no {res}replace
{txt}sl_maria_f already numeric; no {res}replace
{txt}sl_marwari_m already numeric; no {res}replace
{txt}sl_marwari_f already numeric; no {res}replace
{txt}sl_mirgani_m already numeric; no {res}replace
{txt}sl_mirgani_f already numeric; no {res}replace
{txt}sl_mundari_m already numeric; no {res}replace
{txt}sl_mundari_f already numeric; no {res}replace
{txt}sl_muria_m already numeric; no {res}replace
{txt}sl_muria_f already numeric; no {res}replace
{txt}sl_muwasi_m already numeric; no {res}replace
{txt}sl_muwasi_f already numeric; no {res}replace
{txt}sl_nagarchi_m already numeric; no {res}replace
{txt}sl_nagarchi_f already numeric; no {res}replace
{txt}sl_nahari_m already numeric; no {res}replace
{txt}sl_nahari_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_nimadi_m already numeric; no {res}replace
{txt}sl_nimadi_f already numeric; no {res}replace
{txt}sl_oriya_m already numeric; no {res}replace
{txt}sl_oriya_f already numeric; no {res}replace
{txt}sl_pando_m already numeric; no {res}replace
{txt}sl_pando_f already numeric; no {res}replace
{txt}sl_paradhi_m already numeric; no {res}replace
{txt}sl_paradhi_f already numeric; no {res}replace
{txt}sl_pardesi_m already numeric; no {res}replace
{txt}sl_pardesi_f already numeric; no {res}replace
{txt}sl_parji_m already numeric; no {res}replace
{txt}sl_parji_f already numeric; no {res}replace
{txt}sl_parsi_gondi_m already numeric; no {res}replace
{txt}sl_parsi_gondi_f already numeric; no {res}replace
{txt}sl_parwari_m already numeric; no {res}replace
{txt}sl_parwari_f already numeric; no {res}replace
{txt}sl_persian_m already numeric; no {res}replace
{txt}sl_persian_f already numeric; no {res}replace
{txt}sl_punjabi_m already numeric; no {res}replace
{txt}sl_punjabi_f already numeric; no {res}replace
{txt}sl_rajasthani_m already numeric; no {res}replace
{txt}sl_rajasthani_f already numeric; no {res}replace
{txt}sl_sadansadri_m already numeric; no {res}replace
{txt}sl_sadansadri_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_sindhi_m already numeric; no {res}replace
{txt}sl_sindhi_f already numeric; no {res}replace
{txt}sl_surgujia_m already numeric; no {res}replace
{txt}sl_surgujia_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state mp...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str14}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str17}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str523}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str18}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_nepali_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_arabicarbi_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_marathi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_marathi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_maria_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_maria_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bhojpuri_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bhojpuri_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_kharia_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mundari_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mundari_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(46 vars, 166 obs)
subsidiary_speakers_calc_sum_m already numeric; no {res}replace
{txt}subsidiary_speakers_calc_sum_f already numeric; no {res}replace
{txt}subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_angami_m already numeric; no {res}replace
{txt}sl_angami_f already numeric; no {res}replace
{txt}sl_ao_m already numeric; no {res}replace
{txt}sl_ao_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_chakesang_m already numeric; no {res}replace
{txt}sl_chakesang_f already numeric; no {res}replace
{txt}sl_changnaga_m already numeric; no {res}replace
{txt}sl_changnaga_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_khasi_m already numeric; no {res}replace
{txt}sl_khasi_f already numeric; no {res}replace
{txt}sl_kukiunspecified_m already numeric; no {res}replace
{txt}sl_kukiunspecified_f already numeric; no {res}replace
{txt}sl_lotha_m already numeric; no {res}replace
{txt}sl_lotha_f already numeric; no {res}replace
{txt}sl_phom_m already numeric; no {res}replace
{txt}sl_phom_f already numeric; no {res}replace
{txt}sl_rengma_m already numeric; no {res}replace
{txt}sl_rengma_f already numeric; no {res}replace
{txt}sl_sangtam_m already numeric; no {res}replace
{txt}sl_sangtam_f already numeric; no {res}replace
{txt}sl_sema_m already numeric; no {res}replace
{txt}sl_sema_f already numeric; no {res}replace
{txt}sl_yimchungre_m already numeric; no {res}replace
{txt}sl_yimchungre_f already numeric; no {res}replace
{txt}sl_zeliang_m already numeric; no {res}replace
{txt}sl_zeliang_f already numeric; no {res}replace
{err}Appending the parent file to the file for state ng...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str8}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str10}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str598}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str16}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_khasi_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_khasi_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(35 vars, 1,431 obs)
subsidiary_speakers_total_m: byte{res} N{txt} removed; {res}replaced {txt}as {res}long
{txt}(629 missing values generated)
{res}{txt}subsidiary_speakers_total_f: byte{res} N{txt} removed; {res}replaced {txt}as {res}long
{txt}(757 missing values generated)
{res}{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gondi_m already numeric; no {res}replace
{txt}sl_gondi_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_ho_m already numeric; no {res}replace
{txt}sl_ho_f already numeric; no {res}replace
{txt}sl_khond_m already numeric; no {res}replace
{txt}sl_khond_f already numeric; no {res}replace
{txt}sl_kui_m already numeric; no {res}replace
{txt}sl_kui_f already numeric; no {res}replace
{txt}sl_oriya_m already numeric; no {res}replace
{txt}sl_oriya_f already numeric; no {res}replace
{txt}sl_savara_m already numeric; no {res}replace
{txt}sl_savara_f already numeric; no {res}replace
{txt}sl_telugu_m already numeric; no {res}replace
{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state or...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str6}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str15}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str64}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str21}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_gondi_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_gondi_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_gujarati_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_ho_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_ho_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_savara_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_savara_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(28 vars, 42 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_bhotiaunspecified_m already numeric; no {res}replace
{txt}sl_bhotiaunspecified_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_hindustani_m already numeric; no {res}replace
{txt}sl_hindustani_f already numeric; no {res}replace
{txt}sl_lahuli_m already numeric; no {res}replace
{txt}sl_lahuli_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_tibetan_m already numeric; no {res}replace
{txt}sl_tibetan_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state pn...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str6}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str18}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str20}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str18}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:total_speakers_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_nepali_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tibetan_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(26 vars, 772 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_gujarati_m already numeric; no {res}replace
{txt}sl_gujarati_f already numeric; no {res}replace
{txt}sl_marathi_m already numeric; no {res}replace
{txt}sl_marathi_f already numeric; no {res}replace
{txt}sl_sindhi_m already numeric; no {res}replace
{txt}sl_sindhi_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_punjabi_m already numeric; no {res}replace
{txt}sl_punjabi_f already numeric; no {res}replace
{err}Appending the parent file to the file for state rj...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str9}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str14}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str34}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str21}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_gujarati_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_marathi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_marathi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(34 vars, 329 obs)
subsidiary_speakers_main_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_main_total_f already numeric; no {res}replace
{txt}subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_badaga_m already numeric; no {res}replace
{txt}sl_badaga_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_kannada_m already numeric; no {res}replace
{txt}sl_kannada_f already numeric; no {res}replace
{txt}sl_malay_m already numeric; no {res}replace
{txt}sl_malay_f already numeric; no {res}replace
{txt}sl_malayalam_m already numeric; no {res}replace
{txt}sl_malayalam_f already numeric; no {res}replace
{txt}sl_singhalese_m already numeric; no {res}replace
{txt}sl_singhalese_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_telugu_m: all characters numeric; {res}replaced {txt}as {res}int
{txt}(282 missing values generated)
{res}{txt}sl_telugu_f already numeric; no {res}replace
{txt}sl_tulu_m already numeric; no {res}replace
{txt}sl_tulu_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state tn...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str6}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str15}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str21}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str14}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_english_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_telugu_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tulu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tulu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(49 vars, 769 obs)
subsidiary_speakers_calc_sum_m already numeric; no {res}replace
{txt}subsidiary_speakers_calc_sum_f already numeric; no {res}replace
{txt}subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_burmese_m already numeric; no {res}replace
{txt}sl_burmese_f already numeric; no {res}replace
{txt}sl_chakma_m already numeric; no {res}replace
{txt}sl_chakma_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_halam_m already numeric; no {res}replace
{txt}sl_halam_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_jamatia_m already numeric; no {res}replace
{txt}sl_jamatia_f already numeric; no {res}replace
{txt}sl_kalai_m already numeric; no {res}replace
{txt}sl_kalai_f already numeric; no {res}replace
{txt}sl_karbong_m already numeric; no {res}replace
{txt}sl_karbong_f already numeric; no {res}replace
{txt}sl_lushaimizo_m already numeric; no {res}replace
{txt}sl_lushaimizo_f already numeric; no {res}replace
{txt}sl_manipurimeithei_m already numeric; no {res}replace
{txt}sl_manipurimeithei_f already numeric; no {res}replace
{txt}sl_mogh_m already numeric; no {res}replace
{txt}sl_mogh_f already numeric; no {res}replace
{txt}sl_mundaunspecified_m already numeric; no {res}replace
{txt}sl_mundaunspecified_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_persiana_m already numeric; no {res}replace
{txt}sl_persian_f already numeric; no {res}replace
{txt}sl_reang_m already numeric; no {res}replace
{txt}sl_reang_f already numeric; no {res}replace
{txt}sl_rupini_m already numeric; no {res}replace
{txt}sl_rupini_f already numeric; no {res}replace
{txt}sl_tripuri_m already numeric; no {res}replace
{txt}sl_tripuri_f already numeric; no {res}replace
{err}Appending the parent file to the file for state tr...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str7}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str24}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str27}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str17}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:subsidiary_speakers_total_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_nepali_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bengali_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_burmese_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_lushaimizo_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_lushaimizo_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_manipurimeithei_m} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_manipurimeithei_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_chakma_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_reang_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}
{res}{text}(96 vars, 2,484 obs)
subsidiary_speakers_total_m already numeric; no {res}replace
{txt}subsidiary_speakers_total_f already numeric; no {res}replace
{txt}sl_adibhasha_m already numeric; no {res}replace
{txt}sl_adibhasha_f already numeric; no {res}replace
{txt}sl_arabicarabi_m already numeric; no {res}replace
{txt}sl_arabicarabi_f already numeric; no {res}replace
{txt}sl_assamese_m already numeric; no {res}replace
{txt}sl_assamese_f already numeric; no {res}replace
{txt}sl_bengali_m already numeric; no {res}replace
{txt}sl_bengali_f already numeric; no {res}replace
{txt}sl_bhotia_unspecified_m already numeric; no {res}replace
{txt}sl_bhotia_unspecified_f already numeric; no {res}replace
{txt}sl_bodoboro_m already numeric; no {res}replace
{txt}sl_bodoboro_f already numeric; no {res}replace
{txt}sl_burmese_m already numeric; no {res}replace
{txt}sl_burmese_f already numeric; no {res}replace
{txt}sl_chinesechini_m already numeric; no {res}replace
{txt}sl_chinesechini_f already numeric; no {res}replace
{txt}sl_dogri_m already numeric; no {res}replace
{txt}sl_dogri_f already numeric; no {res}replace
{txt}sl_dukpa_m already numeric; no {res}replace
{txt}sl_dukpa_f already numeric; no {res}replace
{txt}sl_english_m already numeric; no {res}replace
{txt}sl_english_f already numeric; no {res}replace
{txt}sl_french_m already numeric; no {res}replace
{txt}sl_french_f already numeric; no {res}replace
{txt}sl_garo_m already numeric; no {res}replace
{txt}sl_garo_f already numeric; no {res}replace
{txt}sl_hindi_m already numeric; no {res}replace
{txt}sl_hindi_f already numeric; no {res}replace
{txt}sl_karmali_m already numeric; no {res}replace
{txt}sl_karmali_f already numeric; no {res}replace
{txt}sl_khaira_m already numeric; no {res}replace
{txt}sl_khaira_f already numeric; no {res}replace
{txt}sl_kodakora_m already numeric; no {res}replace
{txt}sl_kodakora_f already numeric; no {res}replace
{txt}sl_kurukhoraon_m already numeric; no {res}replace
{txt}sl_kurukhoraon_f already numeric; no {res}replace
{txt}sl_lepcha_m already numeric; no {res}replace
{txt}sl_lepcha_f already numeric; no {res}replace
{txt}sl_limbu_m already numeric; no {res}replace
{txt}sl_limbu_f already numeric; no {res}replace
{txt}sl_madhesi_m already numeric; no {res}replace
{txt}sl_madhesi_f already numeric; no {res}replace
{txt}sl_malpaharia_m already numeric; no {res}replace
{txt}sl_malpaharia_f already numeric; no {res}replace
{txt}sl_mangari_m already numeric; no {res}replace
{txt}sl_mangari_f already numeric; no {res}replace
{txt}sl_mech_m already numeric; no {res}replace
{txt}sl_mech_f already numeric; no {res}replace
{txt}sl_mundari_m already numeric; no {res}replace
{txt}sl_mundari_f already numeric; no {res}replace
{txt}sl_munda_unspecified_m already numeric; no {res}replace
{txt}sl_munda_unspecified_f already numeric; no {res}replace
{txt}sl_nagari_hindi_m already numeric; no {res}replace
{txt}sl_nagari_hindi_f already numeric; no {res}replace
{txt}sl_nepali_m already numeric; no {res}replace
{txt}sl_nepali_f already numeric; no {res}replace
{txt}sl_oriya_m already numeric; no {res}replace
{txt}sl_oriya_f already numeric; no {res}replace
{txt}sl_paharia_m already numeric; no {res}replace
{txt}sl_paharia_f already numeric; no {res}replace
{txt}sl_persian_m already numeric; no {res}replace
{txt}sl_persian_f already numeric; no {res}replace
{txt}sl_rabha_m already numeric; no {res}replace
{txt}sl_rabha_f already numeric; no {res}replace
{txt}sl_rajbansi_m already numeric; no {res}replace
{txt}sl_rajbansi_f already numeric; no {res}replace
{txt}sl_sadansadri_m already numeric; no {res}replace
{txt}sl_sadansadri_f already numeric; no {res}replace
{txt}sl_sanskrit_m already numeric; no {res}replace
{txt}sl_sanskrit_f already numeric; no {res}replace
{txt}sl_santali_m already numeric; no {res}replace
{txt}sl_santali_f already numeric; no {res}replace
{txt}sl_sherpa_m already numeric; no {res}replace
{txt}sl_sherpa_f already numeric; no {res}replace
{txt}sl_tamang_m already numeric; no {res}replace
{txt}sl_tamang_f already numeric; no {res}replace
{txt}sl_tamil_m already numeric; no {res}replace
{txt}sl_tamil_f already numeric; no {res}replace
{txt}sl_tibetan_m already numeric; no {res}replace
{txt}sl_tibetan_f already numeric; no {res}replace
{txt}sl_turi_m already numeric; no {res}replace
{txt}sl_turi_f already numeric; no {res}replace
{txt}sl_urdu_m already numeric; no {res}replace
{txt}sl_urdu_f already numeric; no {res}replace
{err}Appending the parent file to the file for state wb...
{txt}{p 0 7 2}
(variable
{bf:state} was {bf:str11}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:district} was {bf:str19}, now {bf:str39} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:tribe} was {bf:str64}, now {bf:str600} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:mothertongue} was {bf:str22}, now {bf:str36} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_burmese_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_hindi_f} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tamil_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_urdu_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_tibetan_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_oriya_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_oriya_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_m} was {bf:int}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_assamese_f} was {bf:byte}, now {bf:long} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_kurukhoraon_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_kurukhoraon_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mundari_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mundari_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bodoboro_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_bodoboro_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_garo_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_garo_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mech_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_mech_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_rabha_m} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 7 2}
(variable
{bf:sl_rabha_f} was {bf:byte}, now {bf:int} to accommodate using data's values)
{p_end}
{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}

{com}. 
. replace state = trim(state)
{txt}(0 real changes made)

{com}. replace district = trim(district)
{txt}(0 real changes made)

{com}. replace tribe = trim(tribe)
{txt}(0 real changes made)

{com}. 
. *replace district = subinstr(district," district","",1)
. 
. replace mothertongue= trim(itrim(mothertongue))
{txt}(5 real changes made)

{com}. 
. drop if state== "" & district == ""
{txt}(0 observations deleted)

{com}. 
. **replace tribe = "all tribes" if strpos(state,"laccadive")>0
. 
. compress
{txt}  (0 bytes saved)

{com}. 
. save "../08_temp/census1961_st_mothertongue", replace
{txt}{p 0 4 2}
file {bf}
../08_temp/census1961_st_mothertongue.dta{rm}
saved
{p_end}

{com}. 
. project , creates("../08_temp/census1961_st_mothertongue.dta")
{txt}project version2_tribe_language > do-file creates: {res}"08_temp/census1961_st_mothertongue.dta" filesig(1104297003:25208159){txt}

{com}. 
{txt}end of do-file
      name:  {res}plog_766
       {txt}log:  {res}/Users/sreyamajumder/Dropbox/sreya_majumder/tribelanguage_2020 sreya/02_scripts/1.01a_append_census61_language_files.smcl
  {txt}log type:  {res}smcl
 {txt}closed on:  {res} 6 Sep 2021, 20:07:14
{txt}{.-}
{smcl}
{txt}{sf}{ul off}